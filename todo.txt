Анализ дампа на клиенте кнопкой в форме дампа


интегрировать с воллетом
https://bgaming-docs.readme.io/v3.0/reference/create-real-session


В историю ставок добавлять только если fs 0
В стейт всегда.

Если ошибка то дисконектим сокет.


В лаунчТокен только на игре реальной зашивать (таймстемп/60000)-random и по нему определять истечение на клиенте lt=152625128TFGWT515625GGBGAYA162625516
Если для лаунч токена на клиенте в сторе есть плейтокен то коннектиться по нему. Если нет и ланч токен устарел то не коннектиться даже.
На беке лаунч токены хранить в виде очереди и устаревшие по. Таймстемпу из начала очереди удалять или сыгравшие удалять после того как клиенту выслан плей токен

dev мод читы для игры.



Касомизации   только в казино.

Ссылка на игру и валюту, обязательно к заполнению минимум одно из них,
Кастомные поля -  1тоN на запись где Ссылка на экспортируемое поле либо паттерн, минбет и т д. И значение, которое сохраняется в виде текста.

При генерации конфига получаем все кастомизации казино где валюта либо пустая либо совпадает, и игра либо пустая либо совпадает, и поля этой кастомизации применяем к конфигу в приоритете если есть. Иначе из данных game/casino




Убедиться что при новом подходе не выйдет выигрыш за макс экспожур. При макс ставке на всех режимах. Добавить ассерт который умножает макс ставку на макс множитель дампа

Игры со статусом 2 сделать видимыми на dev казино

Енв глобальными интерфейсами чтобы в проекте можно было расширить.

_nodes скрывать draftable поле если не установлено  storeForm
пофиксить перемещение полей. 
draftable переместить рядом с storeForm

Поле id зоздавать полностью невидимым и с классом monospace

автосохранение файлов сделать сохранением файла в БД. Очистку тоже не забыть.

Deploy.ts сделать дампинг через
pg_dump --schema-only -t _users
pg_dump -t _nodes




field_roles_view, field_roles_create, field_roles_update  список ролей имеющих право на операции с полем


serverOn hiPriority

Сделать безопасный выход с ожиданием всех запросов и закрытием прослушки сокета сразу. При закрытии сейвить сессии при старте загружать сессии.

клик по аватару гостя открывает ошибку. Аватар гостя рендерится в прод билде

ключи у langs не выставляются.

extendEditor провериь перетаскивание  раскоментить dragStart

react-cropper  перенесть обрезку картинки на клиент.
Кроппер сделать зум колесом и драг. Без рамки и отдельной вьюхи

react-datetime - стандартное полe  input type="datetime-local"
react-highlight-words

Вверх вниз у поля переносит за первое видимое

Пагинатору стрелки по бокам вперед назад

Префиксы локализации добавить CORE_

Убрать  n и юзать id  в адресе ноды. Следующый если цифра то рек ид

Попробовать регистрацию из окна рисунка.

проверить обработку и показ ошибок в релизном билде клиента и бека.

проверить просить вход если ответ нет прав и юзер гость  <access>

hide left menu if one non section item only

check if guest try to upload file or image to prevent sessions collision

go to core/ custom event handler by ENV option


ENV enforce login option через запрет гостю доступа к любой ноде кроме логина.

Просмотр и редактирование всего доступа у роли

кнопки перехода к евент хендлерам не подсвечены зеленым в List

Deploy data for _tables by default

сделать деплой релизного билда. версионность по файлам

в релизе настроить CDN заголовки кеша. апи на поддомене.

проверить лишние SQL запросы в логах админки иеонка справа


