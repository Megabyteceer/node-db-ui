
редактирование лукап значения в попапе. не ренедерить в поле

хоткей esc для cancel в форме и попапе
хткей Enter для удаления

savedNode можно выпилить?

скрывать админские контролы в перекрытой форме

админские контрола в модалах пофиксить

регистрация/активация/логин/сброс формы (через ноды, которые не хранят свои данные а только обрабатывают хендлеры)

separate server side and client side isUserHaveRole

Переводы. Стандартные - в коде, кастомные в бд. И грузить их в память. Гет ланг. Метод возвращающий кастомные переводы.

Юзеру поле языка с 0 по умолчанию. Язык системы.

проверить сессии на глючность

убрать <a href="/login

crop падает на прозрачной области. когда кроп больше картинки

требовать nodeRef поле если указано fieldType === FIELD_7_Nto1 15 14

try mysql.createPool

в разделе добавить выбор ссылки на _html

captcha

Generate small icons for all image fields

Lookup nto1 and ntom to user gives access for user if field marked as access opener. Keep such a fields in prevsFields property. Use it in messages.

Linemaster list. render items one by one with delay

проверить все TODO

Use mysql>Alter table students ADD FULLTEXT (first_name, last_name);
mysql>Select * from students where match(first_name, last_name) AGAINST (‘Jones’);

Separate FormEvents and CustomFormEvents. Call both of them.



