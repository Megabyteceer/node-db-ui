


типизовать ретурт getRecords по id 


class TGR {
	/** _nodes */
	async getRecords(nodeId: 1, viewMask: VIEW_MASK, recId: RecId, userSession?: UserSession): Promise<RecordData>;
	/** _docs */
	async getRecords(nodeId: 2, viewMask: VIEW_MASK, recId: null | RecId[], userSession?: UserSession, filterFields?: any, search?: string): Promise<RecordsData>;
	async getRecords(
		_nodeId: RecId,
		_viewMask: VIEW_MASK,
		_recId: null | RecId | RecId[] = null,
		_userSession: UserSession = ADMIN_USER_SESSION,
		_filterFields: any = {},
		_search?: string
	): Promise<RecordData | RecordsData> {
		return '1' as any;
	}
}

const a = new TGR();
const b = a.getRecords;

export { b };

b(





Удалить все d.ts файлы


разделить обработчики событий формы по файлу на ноду и типизовать.
server.on('_nodes.create', (data: InodeRecord, userSession):Result=>{
});
client.on('_nodes.name.change', (data: InodeRecord, userSession):Result=>{
});
Вот так регать неотменяемые хендлеры. Типизовать server и client



подставить типы записей. типизация гетРекордс

preact всместо реакта

переписать все компоненты на типизацию.  У класса геттеры .parent который укорачивает .props.parent

у типа поля тип данных отдельно тип рендерера отдельно.

енум типНоды   раздел. документ. noDBdoc

енум тип поля.  stored  no-stored  client-side-only


енум где рендерить поле. в форме, меню, футере, хеадере. социал кнопку переместить в футер

проверить создание раздела.
проверить создание поля.


field_roles_view, create, update

права доступа проверять только в crud запросах на входе от клиента. А не в функциях записи чтения удаления апдейта записей в бд. Эти функции вызываются из хендлеров и там доступ весь всегда. Юзер туда передается только для привязки юзера и группы.

аюекапить базу и Сканануть https://sqlmap.org/

перенесть обрезку картинки на клиент.

темная тема

Все возвращаемые хендлеры паренести в афтер апдейт и автеркриейт.

Сделать безопасный выход с ожиданием всех запросов и закрытием прослушки сокета сразу. При закрытии сейвить сессии при старте зпгружать сессии.

клик по аватару гостя открывает ошибку. Аватар гостя рендерится в прод билде

ключи у langs не выставляются.

//@ts-ignore удалить 


Префиксы локализации добавить CORE_

Убрать  n и юзать id  в адресе ноды. Следующый если цифра то рек ид

Попробовать регистрацию из окна рисунка.

проверить обработку и показ ошибок в релизном билде клиента и бека.

проверить просить вход если ответ нет прав и юзер гость  <access>



hide left menu if one non section item only

check if guest try to upload file or image to prevent sessions collision

go to core/ custom event handler by ENV option


ENV enforce login option

Просмотр и редактирование всего доступа у роли

кнопки перехода к евент хендлерам не подсвечены зеленым в List

Deploy data for _tables by default

сделать деплой релизного билда. версионность по файлам

в релизе настроить CDN заголовки кеша. апи на поддомене.



