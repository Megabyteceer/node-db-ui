создание n2m само на себя

создание всех типов полей
отображение всех типов
проверить отправку всех полей

создание всех видов разделов.
по умолчанию сортировать старые в конце

переименовать pre  post
в UPDATE передавать fullNewData?



search brokes react

getData
postData move to promises

code editor не ескейпить добавить __noescape field in to request and use it in src sendRemove all \r symbols before send and in alll sources

replace callsCount

Save subform on save parent form when edit enum entry. check if subform valid

Hide enum value parent field if it subform

Add classes for all elements. Move styles to css.
Make selenium tests

модальные формы для редактирования админских вещей. (admin.popup заменить)

регистрация/активация/логин/сброс формы (через ноды, которые не хранят свои данные а только обрабатывают хендлеры)

separate server side and client side isUserHaveRole

добавить данные о настройках сервера в getTree (проверить все TODO)

Переводы. Стандартные - в коде, кастомные в бд. И грузить их в памать. Гет ланг. Метод возвращающий кастомные переводы.

Юзеру поле языка с 0 по умолчанию. Язык стстемы.

Maintenance mode ansver immideatelly with special code and client will resend request later.Count current requests in progress. And stop after zero only.

проверить сессии на глючность

убрать <a href="/login

запретить подчеркивания не в начале названия поля. чтобы _en не добавили.
запретить подчеркивание в названии таблиц

Check editable fields visible before prehandler, but alliow to set them all in handlers. Check data for unknown fields in debug
Скрыть icon,selectFieldName поле фиелдс таким рбразоми 

Combine sqls in field edit. Node edit.

crop падает на прозрачной области. когда кроп больше картинки

в релизе CodeMirror импортировать по другому

требовать nodeRef поле если указано nodeRef === FIELD_7_Nto1

try mysql.createPool

shake alerts on аppearing

w h поля для картинки

в разделе добавить выбор ссылки на _html

captcha

Generate small icons for all image fields

Lookup to user gives access for user if field marked as access opener. Keep such a fields in prevsFields property. Use it in messages.

Linemaster list. render items one by one with delay